#language: python
#python:
#  - "2.6"#
#  - "2.7"
# command to install dependencies
#install: "pip install -r requirements.txt"
# command to run tests
#script: "nosetests --with-coverage --cover-package=api --cover-branches --cover-erase --cover-html --cover-html-dir=cover --exe"
